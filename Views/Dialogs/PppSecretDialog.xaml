<Window x:Class="MikrNSN.Views.Dialogs.PppSecretDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PPP Secret" Height="320" Width="420" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" x:Name="NameText"/>

        <TextBlock Grid.Row="1" Text="Password" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="PasswordText"/>

        <TextBlock Grid.Row="2" Text="Profile" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="ProfileText"/>

        <TextBlock Grid.Row="3" Text="Comment" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="CommentText"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="100" Margin="8" Click="Ok_Click"/>
            <Button Content="Cancel" Width="100" Margin="8" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
