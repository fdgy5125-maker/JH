<Window x:Class="MikrNSN.Views.Dialogs.PppProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PPP Profile" Height="360" Width="460" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Name" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" x:Name="NameText"/>

        <TextBlock Grid.Row="1" Text="Local Address" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="LocalText"/>

        <TextBlock Grid.Row="2" Text="Remote Address" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="RemoteText"/>

        <TextBlock Grid.Row="3" Text="Rate Limit" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="RateText"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="100" Margin="8" Click="Ok_Click"/>
            <Button Content="Cancel" Width="100" Margin="8" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
